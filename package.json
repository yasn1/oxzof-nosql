{
  "name": "oxzof-nosql",
  "version": "1.0.0",
  "description": "A lightweight NoSQL-style database built on SQLite. Stores JSON data with auto-increment _id and supports MongoDB-like operations (insertOne, find, findOne). Ideal for simple, file-based NoSQL solutions.",
  "main": "database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "path": "^0.12.7",
    "sqlite3": "^5.1.7"
  }
}
